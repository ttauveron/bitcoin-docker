Get a running shell with autocompletion

``` shell
docker-compose exec --user bitcoin bitcoin-core bash -l
```

Generate password hash : https://github.com/bitcoin/bitcoin/blob/master/share/rpcauth/rpcauth.py
